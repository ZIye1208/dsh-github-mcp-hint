// dsh-github-mcp-hint host half: the behavior lives entirely in the client half.
// An empty apply is enough to make the row a valid Node-side plugin entry.
function apply() {}
export { apply as default };